# Project-wide Gradle settings
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# Android
android.useAndroidX=true
android.nonTransitiveRClass=true

# Kotlin
kotlin.code.style=official
kotlin.js.yarn=false

# ---------------------------------------------------------------------------
# Maven Central publishing (com.vanniktech.maven.publish)
# ---------------------------------------------------------------------------
# Coordinates shared by every published module. Per-module artifact ids / names
# live in each module's own gradle.properties.
GROUP=com.latenighthack.ktflags
VERSION_NAME=0.1.0

SONATYPE_HOST=CENTRAL_PORTAL
SONATYPE_AUTOMATIC_RELEASE=true
RELEASE_SIGNING_ENABLED=true

# Shared POM metadata.
POM_INCEPTION_YEAR=2026
POM_URL=https://github.com/mproberts/ktflags

POM_LICENSE_NAME=The Apache Software License, Version 2.0
POM_LICENSE_URL=https://www.apache.org/licenses/LICENSE-2.0.txt
POM_LICENSE_DIST=repo

POM_SCM_URL=https://github.com/mproberts/ktflags
POM_SCM_CONNECTION=scm:git:git://github.com/mproberts/ktflags.git
POM_SCM_DEV_CONNECTION=scm:git:ssh://git@github.com/mproberts/ktflags.git

POM_DEVELOPER_ID=mproberts
POM_DEVELOPER_NAME=Mike Roberts
POM_DEVELOPER_URL=https://github.com/mproberts
POM_DEVELOPER_EMAIL=mike@mpr.io

# ktflags dogfoods its own Gradle plugin (included build ktflags-gradle-plugin): the demos resolve
# the KSP processor via project() instead of the published Maven coordinate. External consumers do
# not set this, so they get the coordinate at the plugin's version.
ktflags.useProjectDependencies=true
